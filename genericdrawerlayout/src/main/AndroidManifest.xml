<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.genericdrawerLayout">

    <!--<application
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".simple.GenericDrawerActivity"
            android:label="@string/app_name" />
        <activity android:name=".simple.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".simple.MaterialMenuActivity" />
    </application>-->
    <application>
        <activity android:name="com.genericdrawerLayout.simple.MaterialMenuActivity" />
        <activity
            android:name="com.genericdrawerLayout.simple.GenericDrawerActivity"
            android:label="@string/app_name" />
    </application>

</manifest>
